<h1>パスワードリセット申請</h1>
<%= form_with url: password_resets_path, local: true, method: :post do |f| %>
    <%= f.label :email, "メールアドレス" %>
    <%= f.email_field :email %>
    <%= f.submit "送信する" %>
<% end %>
