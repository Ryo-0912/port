$('#<%= button-edit-#{@genre.id} %>').html('<%= form_tag("/genres/#{@genre.id}/update") do %>
  <div class="form-wrp form-for-index">
    <div class="form newpost">
      <input type="text">
        <%= @genre.name %>
      </input>
      <input type="submit" value="保存">
    </div>
  </div>
<% end %>')